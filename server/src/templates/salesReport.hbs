<!-- Handlebars template สำหรับ render เป็น PDF ผ่าน Puppeteer (report.controller.ts) -->
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="utf-8" />
  <style>
    /* @page กำหนดขนาดกระดาษ + margin จริงให้ Puppeteer ใช้ตอน print (preferCSSPageSize: true ใน pdf.util.ts) */
    @page { size: A4; margin: 16mm 12mm; }
    /* 265mm = A4 297mm - 16mm บน/ล่างของ @page margin */
    html, body { height: 265mm; margin: 0; }
    body { display: flex; flex-direction: column; box-sizing: border-box; padding: 24px; font-family: 'Tahoma', 'Segoe UI', sans-serif; color: #222; }

    /* header */
    .doc-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 24px; padding-bottom: 16px; border-bottom: 2px solid #222; margin-bottom: 20px; }
    .company-name { font-size: 16px; font-weight: 700; margin: 0 0 4px; }
    .company-detail { font-size: 11px; color: #555; line-height: 1.5; }
    .doc-title { flex-shrink: 0; max-width: 260px; text-align: right; }
    .doc-title h1 { font-size: 19px; margin: 0 0 4px; }
    .doc-title .meta { font-size: 11px; color: #666; margin-top: 2px; }

    /* content */
    .doc-content { flex: 1 0 auto; }
    .summary { display: flex; gap: 24px; margin-bottom: 20px; font-size: 13px; }
    .summary div { border: 1px solid #ddd; border-radius: 6px; padding: 8px 12px; }
    .summary .label { color: #666; font-size: 11px; }
    .summary .value { font-size: 15px; font-weight: 700; }
    table { width: 100%; border-collapse: collapse; font-size: 12px; }
    th { padding: 8px; text-align: left; font-weight: 700; border-bottom: 2px solid #222; }
    td { padding: 8px; }
    tbody tr:nth-child(even) { background: #f7f7f7; }
    .text-right { text-align: right; }

    /* footer */
    .doc-footer-wrap { margin-top: auto; padding-top: 16px; }
    .doc-footer { padding-top: 16px; border-top: 1px dashed #bbb; font-size: 12px; }
    .doc-footer .label { font-weight: 700; color: #222; margin-bottom: 4px; }
    .amount-in-words { font-size: 12px; font-weight: 700; }
    .footer-note { margin-top: 20px; font-size: 10px; color: #999; text-align: center; }
  </style>
</head>
<body>
  <div class="doc-header">
    <div>
      <p class="company-name">บริษัท คลังสินค้า จำกัด (สมมติ)</p>
      <div class="company-detail">
        123 ถนนสุขุมวิท แขวงคลองตัน เขตคลองเตย กรุงเทพฯ 10110<br />
        โทร. 02-123-4567 · เลขประจำตัวผู้เสียภาษี 0-0000-00000-00-0
      </div>
    </div>
    <div class="doc-title">
      <h1>รายงานยอดขาย (Sales Report)</h1>
      <div class="meta">สร้างเมื่อ {{formatDate generatedAt}}</div>
      {{#if dateRangeText}}<div class="meta">ช่วงวันที่ {{dateRangeText}}</div>{{/if}}
    </div>
  </div>

  <div class="doc-content">
    <div class="summary">
      <div><div class="label">ยอดขายรวม</div><div class="value">{{formatCurrency totalAmount}} บาท</div></div>
      <div><div class="label">จำนวน Invoice</div><div class="value">{{invoiceCount}}</div></div>
    </div>

    <table>
      <thead>
        <tr>
          <th>Invoice Number</th>
          <th>Order Number</th>
          <th>Status</th>
          <th class="text-right">Total Amount</th>
          <th>Created At</th>
        </tr>
      </thead>
      <tbody>
        {{#each rows}}
        <tr>
          <td>{{this.invoiceNumber}}</td>
          <td>{{this.orderNumber}}</td>
          <td>{{this.orderStatus}}</td>
          <td class="text-right">{{formatCurrency this.totalAmount}}</td>
          <td>{{formatDate this.createdAt}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

  <div class="doc-footer-wrap">
    <div class="doc-footer">
      <div class="label">ยอดขายรวมเป็นตัวอักษร</div>
      <div class="amount-in-words">({{formatBahtText totalAmount}})</div>
    </div>

    <div class="footer-note">เอกสารนี้ออกโดยระบบอัตโนมัติ · พิมพ์เมื่อ {{formatDate generatedAt}}</div>
  </div>
</body>
</html>
